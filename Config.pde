// Configurações do jogo //

// Proporções da tela
static final int gridX = 24;
static final int gridY = 16;

// Tamanho do grid
static final int gridSize = 48;
static final int initialMoney = 1000;
static final int initialHealth = 1;

// Caminho
static final int nPath = 3;
final color pathColor = color(200, 200, 0); // defina a cor em rgb
final color selectedPathColor = color(255, 255, 0); // defina a cor em rgb

static final int maxPathSize = (int)(gridX+gridY);
// Interface //


// Texto
static final String DEFAULT_FONT_NAME = "fonts/arial.ttf";
static final int DEFAULT_FONT_SIZE = 16;
PFont DEFAULT_TEXT_FONT;
